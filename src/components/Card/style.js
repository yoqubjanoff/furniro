import styled from "styled-components";

export const Container = styled.div`
position: absolute;
right: 0;
left: 0;
bottom: 0;
top: 0;
z-index: 10;
background-color: rgba(0, 0, 0, 0.20);
`;

Container.Wrapper = styled.div``;
Container.Header = styled.div``;
Container.Title = styled.p``;
Container.IconBug = styled.img``;
Container.ImageTitleBox = styled.div``;
Container.ImageBox = styled.div``;
Container.Image = styled.img``;
Container.TitleDescBox = styled.div``;
Container.NameProduct = styled.p``;
Container.NumNameBox = styled.div``;
Container.Num = styled.span``;
Container.Eks = styled.span``;
Container.Price = styled.span``;
Container.IconDelet = styled.img``;